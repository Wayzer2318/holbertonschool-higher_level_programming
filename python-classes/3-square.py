#!/usr/bin/python3
""" Square """
class Square:
    """ Square """
    def __init__(self, size=0):
        self.name = size
        if type(size) != int:
            raise TypeError("size must be an integer")
        if rize < 0:
            raise ValueError("size must be >= 0")

    def area(self):
        return size**size
    
