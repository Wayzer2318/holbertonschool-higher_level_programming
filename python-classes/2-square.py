#!/usr/bin/python3
""" square """


class Square:
    """ square """
    def __init__(self, name):
        self._Square__size = name
    try:
        type(size) = int
    except TypeError:
        print("size must be an integer")

    try:
        size < à
    except ValueError:
        print("size must be >= 0")
p = Square('size')
