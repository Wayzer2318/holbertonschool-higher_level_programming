#!/usr/bin/python3
""" square """


class Square:
    """ square """
    def __init__(self, name):
        self._Square__size = name

p = Square('size')

try:
    type(size) = int
except TypeError:
    print("size must be an integer")

try:
    size < à
except ValueError:
    print("size must be >= 0")
